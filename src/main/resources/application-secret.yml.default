spring:
  security:
    oauth2:
      client:
        registration:
          kakao:
            clientId: NEED_TO_INPUT

custom:
  jwt:
    secretKey: NEED_TO_INPUT
  accessToken:
    expirationSeconds: "#{60 * 10}"
  refreshToken:
    expirationSeconds: "#{60 * 60 * 24}"